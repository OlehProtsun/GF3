<UserControl x:Class="WPFApp.View.Availability.AvailabilityProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dt="clr-namespace:WPFApp.View.Availability.DesignTime"
             xmlns:sys="clr-namespace:System.Windows;assembly=PresentationFramework"
             mc:Ignorable="d"
             d:DesignHeight="830" d:DesignWidth="852"
             d:DataContext="{d:DesignInstance Type=dt:AvailabilityProfileViewDesignVM, IsDesignTimeCreatable=True}"
             Background="{StaticResource PageBackgroundBrush}">

    <!--
        Profile page:
        - ліва картка: основна інформація та дії (cancel/delete)
        - права картка: read-only матриця availability за місяць
    -->
    <Grid ClipToBounds="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TOP CARD (як в AvailabilityEditView) -->
        <Border Grid.Row="0" Style="{StaticResource TopCardStyle}" Margin="9,8,9,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Back button (зверху над текстом) -->
                <Button Grid.Row="0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource BackButtonSmallStyle}"
                        Command="{Binding BackCommand}"
                        Width="62">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="14" Height="14" Margin="0,0,6,0">
                            <Path Data="{StaticResource BackSvgIconGeometry}"
                                  Stretch="Uniform"
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                        </Viewbox>
                        <TextBlock Text="Back"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                    </StackPanel>
                </Button>

                <!-- Title -->
                <TextBlock Grid.Row="1"
                           Margin="0,10,0,0"
                           Text="Availability Profile"
                           Style="{StaticResource AvailabilityPageTitleTextStyle}" />

                <!-- Subtitle -->
                <TextBlock Grid.Row="2"
                           Margin="0,6,0,0"
                           Text="Here you can see a detailed availability profile"
                           Style="{StaticResource AvailabilityPageSubtitleTextStyle}" />
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="9,10,9,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="353" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT CARD: Profile info -->
            <Border Grid.Column="0"
                    Style="{StaticResource CardStyle}"
                    Margin="0,12,0,0"
                    VerticalAlignment="Top">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,8,0">
                            <Path Data="{StaticResource IconInformation}"
                                  Style="{StaticResource AvailabilityHeaderIconPathStyle}" />
                        </Viewbox>
                        <TextBlock Text="Profile Information"
                                   Style="{StaticResource AvailabilitySectionHeaderTextStyle}" />
                    </StackPanel>

                    <!-- ID -->
                    <Grid Grid.Row="1" Margin="0,14,230,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Availability ID:"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="12"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   VerticalAlignment="Center" />

                        <TextBlock Grid.Column="1"
                                   Text="{Binding AvailabilityId}"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="12"
                                   Foreground="Black"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <!-- Name + Month/Year -->
                    <StackPanel Grid.Row="2" Margin="0,14,0,0">
                        <TextBlock Text="Availability Name:"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="12"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding AvailabilityName}"
                                   Margin="0,4,0,0"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="16"
                                   Foreground="Black" />

                        <TextBlock Text="Availability Month-Year:"
                                   Margin="0,12,0,0"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="12"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding AvailabilityMonthYear}"
                                   Margin="0,4,0,0"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="16"
                                   Foreground="Black" />
                    </StackPanel>

                    <!-- Bottom buttons -->
                    <Grid Grid.Row="4" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button Width="84"
                                Height="33"
                                Style="{StaticResource AvailabilityCancelButtonStyle}"
                                Content="Cancel"
                                Command="{Binding CancelProfileCommand}" />

                        <Button Grid.Column="2"
                                Width="86"
                                Height="33"
                                Style="{StaticResource AvailabilityDangerButtonStyle}"
                                Content="Delete"
                                Command="{Binding DeleteCommand}" />
                    </Grid>
                </Grid>
            </Border>

            <!-- RIGHT CARD: Availability table -->
            <Border Grid.Column="2"
                    Style="{StaticResource CardStyle}"
                    Margin="0,12,0,0">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,8,0">
                            <Path Data="{StaticResource IconAvailability}"
                                  Style="{StaticResource AvailabilityHeaderIconPathFlippedStyle}" />
                        </Viewbox>
                        <TextBlock Text="Availability Table"
                                   Style="{StaticResource AvailabilitySectionHeaderTextStyle}" />
                    </StackPanel>

                    <!-- Grid container (як в Edit) -->
                    <Border Grid.Row="1"
                            Margin="0,12,0,12"
                            Style="{StaticResource AvailabilityGridContainerBorderStyle}">
                        <DataGrid x:Name="dataGridAvailabilityMonthProfile"
                          Style="{StaticResource ScheduleMatrixCompactGridStyle_NoTotals}"
                          Background="Transparent"
                          BorderThickness="0"
                          CanUserSortColumns="False"
                          IsReadOnly="True"
                          AutoGenerateColumns="True"
                          ItemsSource="{Binding ProfileAvailabilityMonths}"

                          Focusable="False"
                          IsTabStop="False"

                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.CanContentScroll="True"
                          ColumnWidth="*"
                          MinColumnWidth="140"
                          FrozenColumnCount="1">

                            <DataGrid.Resources>
                                <!-- Прибрати підсвітку selection у WPF DataGrid -->
                                <SolidColorBrush x:Key="{x:Static sys:SystemColors.HighlightBrushKey}" Color="Transparent" />
                                <SolidColorBrush x:Key="{x:Static sys:SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />

                                <!-- І щоб текст НЕ ставав білим/іншим при selection -->
                                <SolidColorBrush x:Key="{x:Static sys:SystemColors.HighlightTextBrushKey}"
                         Color="{DynamicResource {x:Static sys:SystemColors.ControlTextColorKey}}" />
                                <SolidColorBrush x:Key="{x:Static sys:SystemColors.InactiveSelectionHighlightTextBrushKey}"
                         Color="{DynamicResource {x:Static sys:SystemColors.ControlTextColorKey}}" />

                                <Style TargetType="TextBlock" BasedOn="{StaticResource MatrixTextBlockStyle}" />
                                <Style TargetType="CheckBox" BasedOn="{StaticResource CenterCheckBoxStyle}" />
                            </DataGrid.Resources>

                        </DataGrid>


                    </Border>

                    <!-- Bottom buttons -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button Width="84"
                                Height="33"
                                Style="{StaticResource AvailabilityCancelButtonStyle}"
                                Content="Cancel"
                                Command="{Binding CancelTableCommand}" />

                        <Button Grid.Column="2"
                                Width="84"
                                Height="33"
                                Style="{StaticResource RoundedButtonStyle}"
                                Content="Edit"
                                Command="{Binding EditCommand}" />
                    </Grid>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
