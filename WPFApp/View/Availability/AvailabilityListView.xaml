<UserControl x:Class="WPFApp.View.Availability.AvailabilityListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dt="clr-namespace:WPFApp.View.Availability.DesignTime"
             mc:Ignorable="d"
             d:DesignHeight="830" d:DesignWidth="852"
             d:DataContext="{d:DesignInstance Type=dt:AvailabilityListViewDesignVM, IsDesignTimeCreatable=True}"
             Background="White">

    <UserControl.Resources>

        <!-- Rounded button (аналог Guna2Button BorderRadius=12) -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="37"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#FF3347FF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon button (btnSearch: 37x37, LightGray, radius 12) -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="37"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Background" Value="#FFD3D3D3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded TextBox + watermark -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="37"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Silver"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border CornerRadius="12"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>

                            <TextBlock x:Name="Watermark"
                                       Text="Enter search value"
                                       Foreground="LightGray"
                                       Margin="14,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF5E94FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid style: як ConfigureMonthGrid -->
        <Style x:Key="AvailabilityListGridStyle" TargetType="DataGrid">
            <!-- WinForms: AutoGenerateColumns=false, ReadOnly=true, FullRowSelect, Fill -->
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>

            <!-- прибираємо зайве -->
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>

            <!-- вигляд -->
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="Background" Value="Gainsboro"/>

            <!-- WinForms: ColumnHeadersHeight=36 -->
            <Setter Property="ColumnHeaderHeight" Value="36"/>

            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,0,8,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <!-- WinForms: RowTemplate.Height=36 + DividerHeight=6 -->
            <!-- DividerHeight імітуємо через Margin знизу -->
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="36"/>
                        <Setter Property="Margin" Value="0,0,0,6"/>
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <!-- WinForms: DefaultCellStyle.Padding = (8,6,8,6) -->
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,6,8,6"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <!-- у тебе в ApplyAvailabilityGridLook selection Gray/White -->
                                <Setter Property="Background" Value="Gray"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top card -->
        <Border Grid.Row="0"
                Margin="5,0,5,0"
                Height="111"
                Background="White"
                CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.22"/>
            </Border.Effect>

            <Grid>
                <TextBlock Text="Availability List"
                           FontFamily="Segoe UI"
                           FontSize="16"
                           Foreground="Black"
                           Margin="8,9,0,0"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"/>

                <TextBlock Text="Here you will see a list of all existing availability"
                           FontFamily="Segoe UI"
                           FontSize="12"
                           Foreground="Silver"
                           Margin="10,39,0,0"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"/>

                <Grid Margin="6,68,6,0"
                      Height="37"
                      VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="37"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="86"/>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="86"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Search"
                            Command="{Binding SearchCommand}">
                        <Viewbox Width="15" Height="15">
                            <Canvas Width="24" Height="24">
                                <Ellipse Width="14" Height="14"
                                         Stroke="White"
                                         StrokeThickness="2"
                                         Canvas.Left="2" Canvas.Top="2"/>
                                <Line X1="14" Y1="14" X2="22" Y2="22"
                                      Stroke="White"
                                      StrokeThickness="2"/>
                            </Canvas>
                        </Viewbox>
                    </Button>

                    <TextBox x:Name="inputSearch"
                             Grid.Column="2"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                    <Button Grid.Column="4"
                            Style="{StaticResource RoundedButtonStyle}"
                            Command="{Binding AddNewCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="＋" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock Text="Add New" FontSize="12"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="6"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="#FF3347FF"
                            Command="{Binding EditCommand}">
                        <TextBlock Text="Edit" FontSize="12"/>
                    </Button>

                    <Button Grid.Column="8"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="Firebrick"
                            Command="{Binding DeleteCommand}">
                        <TextBlock Text="Delete" FontSize="12"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Grid area -->
        <Border Grid.Row="1"
                Margin="15,21,12,15"
                CornerRadius="20"
                Background="Gainsboro"
                ClipToBounds="True">
            <DataGrid x:Name="dataGridAvailabilityList"
                      Style="{StaticResource AvailabilityListGridStyle}"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem}">
                <!-- WinForms: 1 колонка -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Availability Name"
                                        Binding="{Binding Name}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
