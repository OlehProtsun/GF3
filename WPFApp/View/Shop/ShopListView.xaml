<UserControl x:Class="WPFApp.View.Shop.ShopListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="830" d:DesignWidth="852"
             Background="{StaticResource PageBackgroundBrush}">

        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top card -->
        <Border Grid.Row="0" Style="{StaticResource TopCardStyle}" Margin="9,8,9,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Shop List"
                           Style="{StaticResource AvailabilityPageTitleTextStyle}" />

                <TextBlock Grid.Row="1"
                           Margin="0,6,0,10"
                           Text="Here you will see a list of all shops"
                           Style="{StaticResource AvailabilityPageSubtitleTextStyle}" />

                <!-- Search row -->
                <Grid Grid.Row="2" Height="37">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="37" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Search"
                            Command="{Binding SearchCommand}">
                        <Viewbox Width="15" Height="15">
                            <Path Data="{StaticResource SearchIcon}"
                                  Style="{StaticResource FlippedSvgPathStyle}"
                                  Fill="Black" />
                        </Viewbox>
                    </Button>

                    <TextBox Grid.Column="2"
                             Style="{StaticResource CompactRoundedTextBoxWithPlaceholderStyle}"
                             Tag="Enter search value"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="4"
                            Style="{StaticResource RoundedButtonStyle}"
                            Height="37"
                            Padding="10,0"
                            Command="{Binding AddNewCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="＋" FontSize="14" Margin="0,0,6,0" />
                            <TextBlock Text="Add New" FontSize="12" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Shop card -->
        <Border Grid.Row="1"
                Margin="7,21,7,7"
                Style="{StaticResource AvailabilitySectionCardStyle}">

            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.22" />
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,8,0">
                            <Path Data="{StaticResource IconShop}"
                                  Style="{StaticResource FlippedSvgPathStyle}"
                                  Fill="Black" />
                        </Viewbox>

                        <TextBlock Text="Shops"
                                   Style="{StaticResource AvailabilitySectionHeaderTextStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Style="{StaticResource AvailabilityBadgeBorderStyle}"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0">
                            <TextBlock Style="{StaticResource AvailabilityBadgeTextStyle}"
                                       Text="{Binding Items.Count, StringFormat=Total: {0}}" />
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- GRID CONTAINER -->
                <Border Grid.Row="1"
                        Style="{StaticResource AvailabilityGridContainerBorderStyle}">

                    <DataGrid x:Name="dataGridShopList"
                              Style="{StaticResource ShopListCardGridStyle}"
                              Background="Transparent"
                              BorderThickness="0"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"
                                                Binding="{Binding Name}"
                                                Width="*" />
                            <DataGridTextColumn Header="Address"
                                                Binding="{Binding Address}"
                                                Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
