<UserControl x:Class="WPFApp.View.Shop.ShopEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validation="clr-namespace:WPFApp.Converters.Validation"
             mc:Ignorable="d"
             d:DesignHeight="830" d:DesignWidth="852"
             Background="{StaticResource PageBackgroundBrush}">

    <UserControl.Resources>

        <validation:FirstValidationErrorConverter x:Key="FirstValidationErrorConverter"/>

        <SolidColorBrush x:Key="FieldErrorBorderBrush" Color="#E53935"/>
        <SolidColorBrush x:Key="FieldErrorFillBrush" Color="#FFFFEBEE"/>

        <ControlTemplate x:Key="SquareRedErrorTemplate">
            <Border BorderBrush="{StaticResource FieldErrorBorderBrush}"
                    BorderThickness="2"
                    CornerRadius="2"
                    SnapsToDevicePixels="True">
                <AdornedElementPlaceholder/>
            </Border>
        </ControlTemplate>

        <!-- Override локально, щоб гарантовано був той же вигляд -->
        <Style x:Key="ValidatedRoundedTextBoxStyle"
               TargetType="{x:Type TextBox}"
               BasedOn="{StaticResource RoundedTextBoxStyle}">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource SquareRedErrorTemplate}"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="Background" Value="{StaticResource FieldErrorFillBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource FieldErrorBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self},
                                            Path=(Validation.Errors),
                                            Converter={StaticResource FirstValidationErrorConverter}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>
    
    <Grid ClipToBounds="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TOP CARD -->
        <Border Grid.Row="0" Style="{StaticResource TopCardStyle}" Margin="9,8,9,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Button Grid.Row="0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource BackButtonSmallStyle}"
                        Command="{Binding CancelCommand}"
                        Width="62">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="14" Height="14" Margin="0,0,6,0">
                            <Path Data="{StaticResource BackSvgIconGeometry}"
                                  Stretch="Uniform"
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                        </Viewbox>
                        <TextBlock Text="Back"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                    </StackPanel>
                </Button>

                <TextBlock Grid.Row="1"
                           Margin="0,10,0,0"
                           Text="{Binding FormTitle}"
                           FontFamily="{StaticResource DefaultFontFamily}"
                           FontSize="{StaticResource FontSizePageTitle}"
                           Foreground="Black" />

                <TextBlock Grid.Row="2"
                           Margin="0,6,0,0"
                           Text="{Binding FormSubtitle}"
                           FontFamily="{StaticResource DefaultFontFamily}"
                           FontSize="17"
                           Foreground="{StaticResource SecondaryTextBrush}" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="9,10,9,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource CardStyle}"
                    Margin="0,12,0,0"
                    Grid.Column="1"
                    VerticalAlignment="Top">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Viewbox Width="20" Height="20" Margin="0,0,8,0">
                            <Path Data="{StaticResource IconShop}"
                                  Fill="Black"
                                  Stretch="Uniform"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="-1" />
                                </Path.RenderTransform>
                            </Path>
                        </Viewbox>
                        <TextBlock Text="Shop Information"
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="{StaticResource FontSizeTitle}"
                                   FontWeight="Bold"
                                   Foreground="Black"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Shop ID:"
                           FontFamily="{StaticResource DefaultFontFamily}"
                           FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}"
                           VerticalAlignment="Center" />

                        <TextBlock Grid.Column="1"
                           Text="{Binding ShopId}"
                           FontFamily="{StaticResource DefaultFontFamily}"
                           FontSize="12"
                           Margin="6,0,0,0"
                           Foreground="Black"
                           VerticalAlignment="Center" />
                    </Grid>


                    <StackPanel Grid.Row="2" Margin="0,16,0,0">
                        <!-- Name -->
                        <TextBlock Text="Name"
               FontFamily="{StaticResource DefaultFontFamily}"
               FontSize="12"
               Foreground="{StaticResource MutedTextBrush}" />

                        <Grid>
                            <TextBox x:Name="NameTextBox"
                 Style="{StaticResource ValidatedRoundedTextBoxStyle}"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

                            <TextBlock Text="Enter name..."
                   Margin="12,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="#888888"
                   IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, ElementName=NameTextBox}" Value=""/>
                                                    <Condition Binding="{Binding IsKeyboardFocused, ElementName=NameTextBox}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <!-- Address -->
                        <TextBlock Text="Address"
               Margin="0,12,0,0"
               FontFamily="{StaticResource DefaultFontFamily}"
               FontSize="12"
               Foreground="{StaticResource MutedTextBrush}" />

                        <Grid>
                            <TextBox x:Name="AddressTextBox"
                 Style="{StaticResource ValidatedRoundedTextBoxStyle}"
                 Text="{Binding Address, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

                            <TextBlock Text="Enter address..."
                   Margin="12,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="#888888"
                   IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, ElementName=AddressTextBox}" Value=""/>
                                                    <Condition Binding="{Binding IsKeyboardFocused, ElementName=AddressTextBox}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <!-- Description -->
                        <TextBlock Text="Description"
               Margin="0,12,0,0"
               FontFamily="{StaticResource DefaultFontFamily}"
               FontSize="12"
               Foreground="{StaticResource MutedTextBrush}" />

                        <Grid>
                            <TextBox x:Name="DescriptionTextBox"
                 Style="{StaticResource ValidatedRoundedTextBoxStyle}"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

                            <TextBlock Text="Enter description..."
                   Margin="12,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="#888888"
                   IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, ElementName=DescriptionTextBox}" Value=""/>
                                                    <Condition Binding="{Binding IsKeyboardFocused, ElementName=DescriptionTextBox}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                    <Grid Grid.Row="5" Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button Width="84"
                                Height="33"
                                Style="{StaticResource RoundedButtonStyle}"
                                Background="{StaticResource CancelButtonBackgroundBrush}"
                                Foreground="{StaticResource MutedTextBrush}"
                                Content="Cancel"
                                Command="{Binding CancelCommand}" />

                        <Button Grid.Column="2"
                                Width="86"
                                Height="33"
                                Style="{StaticResource RoundedButtonStyle}"
                                Content="Save"
                                Command="{Binding SaveCommand}" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
