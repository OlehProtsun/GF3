<UserControl x:Class="WPFApp.View.Container.ContainerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="830" d:DesignWidth="852"
             Background="{StaticResource PageBackgroundBrush}">

    <UserControl.Resources>
        <!-- Local resources are not required here:
             typography / card wrappers are now reused from AvailabilityResources.xaml -->
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource TopCardStyle}" Margin="9,8,9,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Page header -->
                <TextBlock Grid.Row="0"
                           Text="Container List"
                           Style="{StaticResource ContainerPageTitleTextStyle}" />

                <TextBlock Grid.Row="1"
                           Margin="0,6,0,10"
                           Text="Here you will see a list of all containers"
                           Style="{StaticResource ContainerPageSubtitleTextStyle}" />

                <Grid Grid.Row="2" Height="37">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="37" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Search"
                            Command="{Binding SearchCommand}">
                        <Viewbox Width="15" Height="15">
                            <Path Data="{StaticResource SearchIcon}"
                                  Style="{StaticResource FlippedSvgPathStyle}"
                                  Fill="Black" />
                        </Viewbox>
                    </Button>

                    <TextBox Grid.Column="2"
                             Style="{StaticResource CompactRoundedTextBoxWithPlaceholderStyle}"
                             Tag="Enter search value"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="4"
                            Style="{StaticResource RoundedButtonStyle}"
                            Height="37"
                            Padding="10,0"
                            Command="{Binding AddNewCommand}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="＋" FontSize="14" Margin="0,0,6,0" />
                            <TextBlock Text="Add New" FontSize="12" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Main content card: shadow wrapper + inner grid host -->
        <Border Grid.Row="1"
                Margin="7,21,7,7"
                Style="{StaticResource ContainerCardShadowHostStyle}">

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Section title + icon -->
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Style="{StaticResource ContainerSectionHeaderStackStyle}">
                        <Viewbox Style="{StaticResource ContainerSectionHeaderIconViewboxStyle}">
                            <Path Data="{StaticResource IconContainer}"
                                  Style="{StaticResource ContainerHeaderIconPathStyle}" />
                        </Viewbox>

                        <TextBlock Text="Containers"
                                   Style="{StaticResource ContainerSectionHeaderTitleTextStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Style="{StaticResource ContainerTotalBadgeBorderStyle}"
                                Margin="0,0,10,0">
                            <TextBlock Style="{StaticResource ContainerTotalBadgeTextStyle}"
                                       Text="{Binding Items.Count, StringFormat=Total: {0}}" />
                        </Border>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1"
                        Style="{StaticResource ContainerInnerGridHostBorderStyle}">

                    <DataGrid x:Name="dataGridContainerList"
                              Style="{StaticResource ContainerListCardGridStyle}"
                              Background="Transparent"
                              BorderThickness="0"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"
                                                Binding="{Binding Name}"
                                                Width="*" />
                            <DataGridTextColumn Header="Note"
                                                Binding="{Binding Note}"
                                                Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
