<UserControl x:Class="WPFApp.View.Information.InformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="2000"
             Background="{StaticResource PageBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource TopCardStyle}"
                Margin="9,8,9,0">
            <TextBlock Text="Information"
                       FontFamily="{StaticResource DefaultFontFamily}"
                       FontSize="{StaticResource FontSizePageTitle}"
                       Foreground="Black" />
        </Border>

        <ScrollViewer Grid.Row="1"
                      Margin="9,10,9,10"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Border Style="{StaticResource CardStyle}"
                    Padding="15"
                    VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Information"
                               FontFamily="{StaticResource DefaultFontFamily}"
                               FontSize="{StaticResource FontSizeTitle}"
                               FontWeight="Bold"
                               Foreground="Black" />

                    <ScrollViewer Margin="0,10,0,0"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  MaxHeight="320">
                        <TextBlock Text="Here you can place information about the app, usage tips, or any notes."
                                   FontFamily="{StaticResource DefaultFontFamily}"
                                   FontSize="{StaticResource FontSizeBase}"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   TextWrapping="Wrap" />
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
