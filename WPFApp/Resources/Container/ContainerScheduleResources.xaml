<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =========================
         Container-specific brushes
         ========================= -->
    <SolidColorBrush x:Key="ScheduleChipAccentBrush" Color="#2F80ED"/>
    <SolidColorBrush x:Key="ScheduleChipAccentHoverBrush" Color="#662F80ED"/>
    <SolidColorBrush x:Key="OverlaySubtleBrush" Color="#14000000"/>
    <SolidColorBrush x:Key="OverlayChipDotBrush" Color="#22000000"/>
    <SolidColorBrush x:Key="OverlayGlyphBrush" Color="#99000000"/>

    <!-- =========================
         Shared tiny layout styles
         ========================= -->
    <Style x:Key="FieldBlockStyle" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,0,10,10"/>
    </Style>

    <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <Style x:Key="InlineSectionCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="{StaticResource Cr20}"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="InlineSectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="BindsGridContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="{StaticResource Cr20}"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>

    <!-- =========================
         Combo popup / tabs scrolling
         ========================= -->
    <ItemsPanelTemplate x:Key="VirtualizingComboItemsPanel">
        <VirtualizingStackPanel />
    </ItemsPanelTemplate>

    <Style x:Key="ComboPopupScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
        <Setter Property="CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False"/>
    </Style>

    <Style x:Key="TabsScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid ClipToBounds="False">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollContentPresenter Grid.Row="0"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                Margin="{TemplateBinding Padding}"
                                                ClipToBounds="False"/>

                        <ScrollBar Grid.Row="1"
                                   Orientation="Horizontal"
                                   Height="10"
                                   Margin="0,6,0,0"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Value="{TemplateBinding HorizontalOffset}"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LeftScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="0"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Style="{StaticResource LeftRoundedScrollBarStyle}"
                                   Margin="0,0,8,0"/>

                        <ScrollContentPresenter Grid.Column="1"
                                                Margin="{TemplateBinding Padding}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RightScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ScrollContentPresenter Grid.Column="0"
                                                Margin="{TemplateBinding Padding}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"/>

                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="1"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Style="{StaticResource LeftRoundedScrollBarStyle}"
                                   Margin="8,0,0,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================
         Buttons / chips
         ========================= -->
    <Style x:Key="SplitMainButtonPressStyle"
           TargetType="Button"
           BasedOn="{StaticResource RoundedButtonStyle}">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ScheduleChipInnerButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource RoundedButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScheduleChipItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{StaticResource SoftGrayBrush}"
                            CornerRadius="{StaticResource Cr10}"
                            Padding="8,5"
                            Margin="0,5,0,5"
                            BorderThickness="1"
                            BorderBrush="Transparent"
                            RenderTransformOrigin="0.5,0.5">

                        <ContentPresenter x:Name="Cp">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="Scale" ScaleX="1" ScaleY="1"/>
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource ScheduleChipAccentBrush}"/>
                        </Trigger>

                        <Trigger Property="IsMouseCaptured" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="0.98" Duration="0:0:0.06"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="0.98" Duration="0:0:0.06"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Scale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1" Duration="0:0:0.08"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Кнопка "+" (замість inline Button.Template в Edit view) -->
    <Style x:Key="ScheduleAddChipButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="36"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Background" Value="{StaticResource ScheduleChipAccentBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource Cr12}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            <Setter TargetName="Bd" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================
         Profile summary styles
         ========================= -->
    <Style x:Key="ScheduleSummaryCellBorderStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="ScheduleSummaryHeaderBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource ScheduleSummaryCellBorderStyle}">
        <Setter Property="Height" Value="45"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style x:Key="ScheduleSummaryHeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="6,0"/>
    </Style>

    <Style x:Key="ScheduleSummaryBodyTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <!-- Базова картка для collapsible side-panel (Profile/Edit) -->
    <Style x:Key="CollapsibleSideCardBaseStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="0,12,0,0"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="MaxHeight" Value="650"/>
    </Style>

    <!-- =========================
     Overlay dim (save-status)
     ========================= -->
    <SolidColorBrush x:Key="ContainerOverlayDimBrush" Color="#40000000"/>

    <!-- =========================
     Page title / subtitle
     (same idea as Availability)
     ========================= -->
    <Style x:Key="ContainerPageTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizePageTitle}" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <Style x:Key="ContainerPageSubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
    </Style>

    <!-- =========================
     Section headers (icon + text)
     same pattern as Availability
     ========================= -->
    <Style x:Key="ContainerSectionHeaderStackStyle" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="ContainerSectionHeaderIconViewboxStyle" TargetType="Viewbox">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Margin" Value="0,0,8,0" />
    </Style>

    <Style x:Key="ContainerHeaderIconPathStyle" TargetType="Path">
        <Setter Property="Fill" Value="Black" />
        <Setter Property="Stretch" Value="Uniform" />
    </Style>

    <Style x:Key="ContainerHeaderIconPathFlippedStyle"
       TargetType="Path"
       BasedOn="{StaticResource ContainerHeaderIconPathStyle}">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="-1" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ContainerSectionHeaderTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeTitle}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- =========================
     Compatibility aliases for refactored Container views
     ========================= -->
    <Style x:Key="ContainerCenterCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource CardStyle}" />

    <Style x:Key="ContainerCardShadowHostStyle"
           TargetType="Border"
           BasedOn="{StaticResource CardStyle}" />

    <Style x:Key="ContainerInnerGridHostBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource AvailabilityGridContainerStyle}" />

    <Style x:Key="ContainerTotalBadgeBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource AvailabilityTotalBadgeBorderStyle}" />

    <Style x:Key="ContainerTotalBadgeTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource AvailabilityTotalBadgeTextStyle}" />

    <Style x:Key="ContainerLabelTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource AvailabilityLabelTextStyle}">
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="ContainerSmallValueTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource AvailabilityValueTextStyle}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="ContainerInputPlaceholderOverlayStyle" TargetType="TextBlock">
        <Setter Property="Text" Value="Enter name..."/>
        <Setter Property="Margin" Value="12,0,0,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="#888888"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
    </Style>

    <Style x:Key="ContainerInputPlaceholderOverlayMultilineStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource ContainerInputPlaceholderOverlayStyle}">
        <Setter Property="Margin" Value="12,10,0,0"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>


    <Style x:Key="ContainerScheduleCollapsibleTitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource ContainerSectionHeaderTitleTextStyle}">
        <Setter Property="Visibility" Value="Visible"/>
        <Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsChecked, ElementName=DetailsToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=EmployeesToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=NoteToggle}" Value="False"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Visibility" Value="Collapsed"/>
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleInfoTitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource ContainerSectionHeaderTitleTextStyle}">
        <Setter Property="Visibility" Value="Visible"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=ScheduleInfoToggle}" Value="False">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleInfoCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource CollapsibleSideCardBaseStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=ScheduleInfoToggle}" Value="False">
                <Setter Property="Height" Value="62"/>
                <Setter Property="Padding" Value="12"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleInfoPanelColumnStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="400"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=ScheduleInfoToggle}" Value="False">
                <Setter Property="Width" Value="140"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleInfoGapColumnStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="10"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=ScheduleInfoToggle}" Value="False">
                <Setter Property="Width" Value="8"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleSummaryVerticalScrollbarFillerBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="Gainsboro"/>
        <Setter Property="BorderThickness" Value="1,0,0,0"/>
        <Setter Property="Visibility" Value="Collapsed"/>
        <Setter Property="Width" Value="0"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding ElementName=SummaryBodyScroll, Path=ComputedVerticalScrollBarVisibility}" Value="Visible">
                <Setter Property="Visibility" Value="Visible"/>
                <Setter Property="Width" Value="{x:Static SystemParameters.VerticalScrollBarWidth}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleEditMainSideColumnStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="380"/>
        <Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsChecked, ElementName=DetailsToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=EmployeesToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=NoteToggle}" Value="False"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Width" Value="140"/>
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ContainerScheduleEditMainGapColumnStyle" TargetType="ColumnDefinition">
        <Setter Property="Width" Value="10"/>
        <Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsChecked, ElementName=DetailsToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=EmployeesToggle}" Value="False"/>
                    <Condition Binding="{Binding IsChecked, ElementName=NoteToggle}" Value="False"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Width" Value="0"/>
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
